# Store

## Backend (already running)

`cd backend && mvn spring-boot:run`

## Admin dashboard

`cd admin && pnpm dev`

## Customer store

`cd store && pnpm dev`

The project is ready for active development! 🚀
